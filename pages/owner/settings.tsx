import React from "react";

import Layout from "common/layout";

const Settings = () => {
  return (
    <Layout title="가게설정">
      <div>Settings</div>
    </Layout>
  );
};

export default Settings;
